import Layout from '../components/PageLayout';
import PlainText from '../components/PlainText';

const Index = () => (
  <div>
    <h1>Test page</h1>
    <p>There should be no styling here!</p>
  </div>
  
);

export default Index;
